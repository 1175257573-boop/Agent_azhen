# 安全策略

## 支持的版本

| 版本 | 是否修复安全 issue |
| --- | --- |
| 1.0.x | ✅ |
| < 1.0 | ❌（公开前的开发版本） |

## 报告漏洞

**请不要开公开 Issue 报告安全漏洞。**

请直接在 GitHub 上走 [Private vulnerability reporting](../../security/advisories/new)，
或发邮件给仓库维护者。收到后我们会在 72 小时内给出首次响应。

报告时请附上：

- 漏洞类型（密钥泄露 / 注入 / 越权 / RCE / 其它）
- 受影响的文件路径与版本
- 最小复现步骤
- 影响面评估

## 使用者的安全注意（重要）

本项目是一个**本地优先**的 Agent 参考实现，默认配置面向开发调试，**不是生产级安全基线**。
公开部署前请务必做以下几件事：

### 1. 本地默认凭据必须换掉

`docker-compose.yml` 里的 Redis / PostgreSQL 使用了 `atlas` / `atlas` 作为账号密码，
这是**给本地 `docker compose up` 用的**，任何暴露到公网或共享网络的环境都必须改。

### 2. API Key 不入库、不进日志

- `.env` 已被 `.gitignore` 排除，提交前请 `git status` 确认
- 优先使用系统环境变量，其次才是 `.env`
- 万一 Key 泄露，第一时间去厂商控制台吊销，本项目无法帮你撤销

### 3. MCP 工具等于让模型执行本地代码

`mcp.json` 里配的每个 MCP Server 都会以你当前用户权限起进程。
**只接你信任的 Server**，不要随便复制网上来的配置。
`agent_kit/mcp_client.py` 提供了 `make_guard` / `make_runtime_guard` 做工具白名单与运行期护栏，建议开启。

### 4. Web 服务默认只监听 127.0.0.1

`python main.py web` 默认绑定 `127.0.0.1:8000`，不对局域网开放。
如果要改绑 `0.0.0.0`，请自行前置鉴权与反向代理 —— 本项目**没有内置任何认证机制**。

### 5. 人工确认（HITL）不要关

涉及写操作（写文件、执行命令、发请求）的工具默认走人工确认。
无人值守场景下关闭 HITL，等于把机器控制权完全交给模型输出，风险自负。

## 已知的设计取舍

| 项 | 现状 | 理由 |
| --- | --- | --- |
| 无内置认证 | FastAPI 层没有登录态 | 定位是本地开发参考实现，接入方自行加 |
| 明文 DSN | `PG_DSN` / `REDIS_URL` 含明文密码 | 与主流框架一致，靠环境变量权限隔离 |
| LangSmith 可选上报 | 配了 `LANGSMITH_API_KEY` 会外发链路数据 | 默认关闭，需要显式配置才启用 |
