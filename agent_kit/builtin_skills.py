"""内置 Skill：「项目严谨工程化」。

Skill 与 MCP 的分工在本文件里体现得很直白：
  · **Skill 负责「怎么查、按什么顺序查、结论怎么组织」**（方法论，活在系统提示里）
  · **MCP 负责「真的去查」**（能力，跑在独立进程里）

所以本 Skill 的第 3 层不是空谈规范，而是直接调用
`atlas-quality-mcp` / `atlas-git-mcp` / `atlas-docs-mcp` 三个 server 取证。
**没有取证就下结论，是工程评审里最典型的失真。**

三层渐进式披露：
  L1 只有名称 + 一句话（常驻提示词，成本几十 token）
  L2 是下面这份完整 SOP（调用 load_skill 才加载）
  L3 按 SOP 调 MCP 工具取证，再按统一格式输出结论
"""

from __future__ import annotations

from agent_kit.multi_agent.skills import Skill

PROJECT_ENGINEERING: Skill = {
    "name": "project_engineering",
    "description": "按工程化规范审查或搭建一个项目：分层、配置、错误、日志、测试、CI、文档、安全八项",
    "content": """
# 项目严谨工程化 · 检查规程

## 0. 铁律（先看这三条）

1. **取证优先**：每一条结论都要有工具输出支撑。禁止凭"看起来不错"下判断。
2. **区分事实与建议**：事实写「是什么 + 在哪」，建议写「建议怎么改 + 为什么」。两者混在一起读者无法决策。
3. **不夸大也不粉饰**：发现问题就报，发现做得好的也要点出来——只报问题的评审没人愿意看第二遍。

## 1. 八项检查清单

| # | 维度 | 合格标准 | 取证工具 |
|---|---|---|---|
| 1 | 分层与依赖方向 | 高层依赖低层，无反向依赖、无循环依赖 | 人工看目录 / `project_tree` |
| 2 | 配置外置 | 密钥只从环境变量读；`.env` 不入库；`.env.example` 必须提交 | `scan_secrets`、`project_checklist` |
| 3 | 统一错误处理 | 有全局异常处理器；不吞异常；错误带上下文 | 人工看代码 |
| 4 | 日志 | 结构化、带 request_id、不打印密钥与完整入参 | `scan_secrets` |
| 5 | 测试 | 零外部依赖可跑；conftest 做环境隔离；钉住易回归的隐性约束 | `check_tests` |
| 6 | CI | lint + test + 冒烟；多版本矩阵；不依赖任何外部服务 | `project_checklist` |
| 7 | 文档 | README/CHANGELOG/LICENSE 齐全；目录锚点有效；改动进 Unreleased | `readme_outline`、`check_anchors`、`changelog_status` |
| 8 | 依赖与安全 | 依赖钉版本；无硬编码密钥；写操作限制在沙箱目录 | `dependency_audit`、`scan_secrets` |

## 2. 执行顺序（不要跳步）

1. **先摸清规模**：`project_code_stats` + `project_tree`，回答「这个项目到底多大、什么结构」。
   规模判断错了，后面所有建议都会失真（对着 2000 行的项目讲分层，是没意义的）。
2. **再看版本现状**：`git_status` + `git_log`，确认在干净的分支上、最近在做什么。
   脏工作区上做评审，结论会和实际代码对不上。
3. **逐项取证**：按第 1 节的表格，一项一项调对应工具，**不要只调自己擅长的那几项**。
4. **汇总输出**：按下节格式给结论。

## 3. 输出格式（固定，不要自由发挥）

```
## 结论
（一句话：合格 / 基本合格但有 N 项阻断 / 不合格）

## 阻断项（必须改）
- [维度] 事实：...  证据：...  建议：...

## 建议项（改了更好）
- ...

## 做得好的地方
- ...

## 未覆盖
- （本次没查、需要人工确认的部分，必须写出来）
```

**「未覆盖」一栏不能省**——评审报告如果不说清自己没查什么，读者会误以为是全量检查。

## 4. 常见反模式（看到就直接判阻断）

- 业务逻辑里直接 `os.getenv` 散落各处 → 配置应集中在一处解析并做类型校验
- `except Exception: pass` → 吞掉的异常会在几周后以更难排查的方式冒出来
- 测试依赖真实 Redis/MySQL/API Key → 别人克隆下来跑不了，CI 也跑不了
- README 写"支持 8 种模式"但实际只有 3 种 → 文档与代码不一致比没文档更糟
- 密钥写进代码但注释"反正是本地项目" → 一旦公开就是事故

## 5. 边界

- 本 Skill **只做只读检查**，不修改任何文件。要改，请人类确认后另开任务。
- 覆盖率、性能、并发这类需要**真实运行**才能回答的问题，不要靠静态扫描猜。
""",
}

# 便于一次性取出全部内置技能
BUILTIN_SKILLS: list[Skill] = [PROJECT_ENGINEERING]
